projects:
  siyudatastore:
    schemaPath: amplify/backend/api/siyudatastore/build/schema.graphql
    includes:
      - /graphql/**/*.js
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: flow
        generatedFileName: src/API.js
        docsFilePath: /graphql
        region: us-east-2
        apiId: null
        maxDepth: 2
extensions:
  amplify:
    version: 3
