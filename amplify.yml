version: 1
backend:
  phases:
    build:
      commands: npm run build
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands: []
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
